On the directory, we can see a compiled file and a token.
The executable take one string parameter
We can see that the program is converting aaaa to abcd
so we just created a program to revert this logic
